export enum DomainEventType {
    USER_CREATED = 'user.created',
    USER_UPDATED = 'user.updated',
    PERFORMANCE_REVIEW_CREATED = 'performance.review_created',
    GOVERNANCE_SYNC_COMPLETED = 'governance.sync_completed',
    NOTIFICATION_CREATED = 'notification.created',
    AUDIT_LOG_REQUESTED = 'audit.log_requested',
    REALTIME_BROADCAST_REQUESTED = 'realtime.broadcast_requested'
}
